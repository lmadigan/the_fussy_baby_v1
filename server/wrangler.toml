name = "fussy-baby-differential"
main = "worker.js"
compatibility_date = "2024-11-01"

# Set your API key as a secret (never commit it):
#   wrangler secret put ANTHROPIC_API_KEY
#
# Optional vars:
[vars]
MODEL = "claude-sonnet-5"
# Lock the proxy to your site once deployed, e.g.:
# ALLOW_ORIGIN = "https://lmadigan.github.io"
